# Data Directory

This directory is reserved for small metadata notes and dataset preparation instructions.

Do not commit raw or processed datasets. Use:

- `data/raw/` for downloaded source datasets
- `data/processed/` for cleaned or converted training/evaluation files

Both directories are ignored by Git.
